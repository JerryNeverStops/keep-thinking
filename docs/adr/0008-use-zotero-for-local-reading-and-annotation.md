# Use Zotero for Local Reading and Annotation

The first version will use Zotero as the maintainer's local reading and annotation environment, with repository files generated from exported annotations. Writing notes directly in Markdown would make static publishing simpler, but it would not provide the desired local reading experience of selecting passages, highlighting, underlining, and adding notes inside a dedicated reader.
